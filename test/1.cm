/* --------------------------------------------------------------------------
This is a program that contains no errors, it is designed to show that with
proper spelling and syntax that all aspects of the C- language are properly
identified and an abstract syntax tree will be constructed.
It will also test that the program can ignore comments properly.
 -------------------------------------------------------------------------- */

/* This is a function declaration comment */
void main(void)
{ /* Comment inside function */
  int x;
  int y;
  int z;
  int a; /* Comment at the end of a line */
  int b[10];

  /* Comment at the start of a line */x = 5;
  y = 10;
  z = y + x;
  b[2] = 5;
  while(x < z)
  {
    if(x < y)
    {
      x = x + 1;
    }
    else
    {
      y = y + 1;
    }
  }
  a = add(x, b);
  return;
}

/*
 * A really long comment that may perhaps
 * span multiple lines
 * just for the purposes of demonstration
*/

int add(int x, int b[])
{
  x = 1;
  return;
}
