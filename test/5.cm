/* --------------------------------------------------------------------------
This program contains an assortment of errors that test different parts of the
grammar and lexical errors to see if the compiler is able to recover or at least
fail gracefully
 -------------------------------------------------------------------------- */

void main(void)
{
  int var;
  void variable;
  int var2;

  /* Lexer does not recognize floating point numbers '.' will result in an error*/
  var = 3.3;
  /* Tests recovery from improper if statement declaration */
  if()
  {
    var = 10 - 6 * 8;
  }

  while(1)
  {
    int max;
    /* Missing semi colon at the end of the stmt */
    max = max + 1
  }

  4 - - 3;

  /* tests the use of function calls in a return stmt */
  return paramsError(var, variable);

}
/* Function declaration with improper parameters */
void paramsError(int a int b)
{

}

/* Function declaration with no type specifier */
f(void)
{
  while
  {

  }
}
